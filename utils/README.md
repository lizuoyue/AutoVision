## Instructions for testing DeepLab v3+ on images

### Command

```
python test_{frozen_graph, saved_model}.py \
  --model ${PATH_TO_THE_MODEL} \
  --input ${PATH_TO_THE_FOLDER_OF_IMAGES} \
  --output ${PATH_FOR_OUTPUT_RESULTS}
```
